spring:
  cloud:
    discovery:
      enabled: true
    config:
      enabled: true
      discovery:
        service-id: config-server
        enabled: true
      fail-fast: false
      password: MyPassword
      username: MyUserName

eureka:
  client:
    serviceUrl:
      defaultZone: http://netflix:eureka@localhost:8761/eureka
