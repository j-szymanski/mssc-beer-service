spring:
  cloud:
    discovery:
      enabled: true
    config:
      discovery:
        service-id: config-server
      fail-fast: true
      password: MyPassword
      username: MyUserName

eureka:
  client:
    serviceUrl:
      defaultZone: http://netflix:eureka@localhost:8761/eureka
